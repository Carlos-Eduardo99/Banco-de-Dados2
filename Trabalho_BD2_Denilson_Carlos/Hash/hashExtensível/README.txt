* Implementação de Índice Baseado em Hashing Extensível;

* O codigo foi executado no VSCode;

* Considerei um numero tamanho 80 para o bucket, para as inserções de até 50000 elementos;

* O tamanho do bucket é variável, mas com muitos elementos é necessário aumentar seu tamanho,
pois ocorrem muitas divisões e o diretório fica muito grande;

* Para usar arquivos CSV diferentes é necessário mudar o nome na linha 13 do arquivo main para o arquivo desejado;

* nas pastas campos, deleção, e inserção tem varios arquivos CSV para testes. É necesario move-los para junto dos arquivos Python.